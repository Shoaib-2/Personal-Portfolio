// Design System Exports
import colors from './colors.jsx';
import typography from './typography.jsx';
import { spacing, containers, breakpoints } from './spacing.jsx';
import { animations, motionVariants } from './animations.jsx';
import shadows from './shadows.jsx';

export const designSystem = {
  colors,
  typography,
  spacing,
  containers,
  breakpoints,
  animations,
  motionVariants,
  shadows,
};

// Individual exports
export { colors, typography, spacing, containers, breakpoints, animations, motionVariants, shadows };

export default designSystem;
